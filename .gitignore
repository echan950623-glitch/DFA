# Build output
node_modules
dist

# Env
.env
.env.local

# Client reference materials (not shipped with the site)
*.pptx
*.pdf
_docs/
pptx_unpacked/

# Claude (dev tooling — never ship to repo/client)
.claude/

# Local devlog (private notes)
DEVLOG.md

# Manual build artifacts (not shipped)
dist-manual/
.manual-build/

# Tooling caches
.playwright-mcp/
.wrangler/
*.log

# Dev / audit scratch (not part of the website)
rwd-audit/
structure.txt
AGENTS.md
TASKS_0428.md

# Archived / unused images (88MB, not shipped)
public/images/unused/
